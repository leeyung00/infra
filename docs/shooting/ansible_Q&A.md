### Q. 테라폼의 .tf 형식에 띄여쓰기가 중요한가?
A. 기본적으로 json 형식이기에 {} 안에 키와 값의 형태로만 존재하면 크게 중요하지 않다.
    하지만 협업 및 유지 보수 관점에서 통일 된 형식이 좋기 때문에 "terraform fmt -recursive && terraform validate" 코드를 통해 정리 및 오류 검증을 습관화 하는 것이 좋다.